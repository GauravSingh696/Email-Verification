{
  "name": "highway-delite-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install && npm install --prefix frontend && npm install --prefix api",
    "vercel-build": "npm run build --prefix frontend"
  }
}